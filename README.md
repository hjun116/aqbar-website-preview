# aqbar-website-preview

**검증용 사본입니다. 이 레포에서 직접 수정하지 마세요.**

- 원본(source of truth): `hjun116/aqbar-website` 의 `claude/local-repo-sync-check-fudw0j` 브랜치 (`index-draft.html`)
- 수정은 항상 원본에서 하고, 이 레포로는 한 방향으로만 복사합니다.
- 프리뷰 주소: https://hjun116.github.io/aqbar-website-preview/
- 이 사본에는 검색 차단(noindex 메타, robots.txt Disallow)이 들어 있습니다. 전환 시 제거 대상입니다.
- `CNAME` 은 의도적으로 없습니다. aqbar.ai 도메인은 원본 레포에만 연결합니다.
- 문의 폼은 실제 Google Apps Script 로 전송됩니다. 테스트 제출 시 이름에 "테스트"를 표기하세요.
